<li id="book">
<%= image_tag(image_path("#{book.title}.jpg"))%>
<%= link_to "Add Book", items_path(:shelf_id => shelf.id,:title => book.title, :year => book.year), method: :post, class:"btn-info btn-sm" %>
<%= book.title %>
<br>
<%= book.author %>
<br>
<%= book.year %>
<br>
<%= book.genre %>
</li>

